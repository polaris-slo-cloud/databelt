apiVersion: v1
kind: Service
metadata:
  name: skylark-api-nodeport
  namespace: default
spec:
  type: NodePort
  selector:
    app: skylark-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30163
  externalTrafficPolicy: Local
